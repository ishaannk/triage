# --- Core (required) ---
fastapi==0.115.5
uvicorn[standard]==0.32.1
httpx==0.27.2
pydantic==2.9.2
pydantic-settings==2.6.1
python-dotenv==1.0.1
pyyaml==6.0.2
numpy==1.26.4

# --- Optional: pgvector retrieval backend (docker-compose Postgres) ---
# Install only if you want the Postgres/pgvector store instead of the local FAISS/numpy fallback.
# psycopg[binary]==3.2.3
# pgvector==0.3.6

# --- Optional: better embeddings for retrieval ---
# If present, the retrieval store uses sentence-transformers; otherwise a dependency-free
# hashing embedding is used so the app runs with zero ML deps.
# sentence-transformers==3.3.1
# faiss-cpu==1.9.0
